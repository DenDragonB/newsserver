# Main config Data of news web-server

# server settings
[server]
port = 8080

# config of database
[database]

host = "localhost"
port = 5432
user = "postgres"
pass = "LionDragon40"
dbname = "newsdb"

# log settings
[logger]
# log file path.
logPath = ""

# minimum level of logging data.
# can be : Debug | Info | Warning | Error
logMinLevel = "Debug"

# Where to output log
# can be : LogToFile | LogToConsole
logTo = "LogToConsole"